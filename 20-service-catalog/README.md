# 20-service-catalog

Will pin `idp-service-catalog`'s XRDs/Compositions/`idp-application` chart to a version
here, per `gitops-strategy.md` §3. Nothing built yet — Phase 2 of the `idp` build (see
`idp/README.md` status). Not wired into the app-of-apps root until there's real content.
